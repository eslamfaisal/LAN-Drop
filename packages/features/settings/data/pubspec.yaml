name: settings_data
description: Data layer for the settings feature.
publish_to: 'none'
resolution: workspace

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  core_domain:
    path: ../../../core/core_domain
  settings_domain:
    path: ../domain
  persistence_kv:
    path: ../../../platform/persistence_kv
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.24.0